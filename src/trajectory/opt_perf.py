import argparse
import csv
import json
import math
import os
import subprocess
from pathlib import Path

import optuna

from ..logger import generate_logger
from .print_perf import parse_log_file

LOGGER = generate_logger(__name__)


def add_subcmd(subparsers):
    """
    mdtbx opt_perf --mdrun-command "gmx mdrun -deffnm prd -gpu_id {gpu_ids}" --gpu 1 2 --omp 4 8 --mpi 1 2
    """
    parser = subparsers.add_parser(
        "opt_perf",
        help="Optimize Gromacs mdrun performance with Optuna",
        formatter_class=argparse.ArgumentDefaultsHelpFormatter,
    )

    command_group = parser.add_mutually_exclusive_group(required=True)
    command_group.add_argument(
        "--command-template",
        type=str,
        help=(
            "Shell command template. Available placeholders: "
            "{trial_dir}, {log_path}, {n_gpu}, {n_core}, {ntomp}, {ntmpi}, {gpu_ids}"
        ),
    )
    command_group.add_argument(
        "--mdrun-command",
        type=str,
        help=(
            "Base mdrun command. If {log_path} or {ntomp} are missing, "
            "'-g {log_path}' and '-ntomp {ntomp}' are appended automatically. "
            "'-ntmpi {ntmpi}' is appended "
            "only for thread-MPI mode."
        ),
    )
    parser.add_argument(
        "--mpi-launcher",
        type=str,
        help=(
            "Optional MPI launcher template prepended to --mdrun-command, "
            "for example 'mpirun -np {ntmpi}' or "
            "'srun --ntasks {ntmpi} --cpus-per-task {ntomp}'."
        ),
    )
    parser.add_argument(
        "--mpi-mode",
        default="auto",
        choices=["auto", "thread_mpi", "external"],
        help=(
            "MPI style. auto treats --mpi-launcher as external MPI and "
            "otherwise uses thread-MPI."
        ),
    )
    parser.add_argument(
        "--log-name",
        default="md.log",
        type=str,
        help="Log file name generated by each trial",
    )
    parser.add_argument(
        "--workdir",
        default="opt_perf_trials",
        type=str,
        help="Working directory for trial subdirectories",
    )
    parser.add_argument(
        "--sampler",
        default="grid",
        choices=["tpe", "random", "grid"],
        help="Optuna sampler",
    )
    parser.add_argument(
        "--seed",
        default=0,
        type=int,
        help="Random seed for Optuna samplers",
    )
    parser.add_argument(
        "--n-trials",
        type=int,
        help="Number of trials. Defaults to full search for grid, otherwise 20",
    )
    parser.add_argument(
        "--n-gpu",
        "--gpu",
        nargs="+",
        type=int,
        default=[1],
        help="Candidate numbers of GPUs",
    )
    parser.add_argument(
        "--n-core",
        "--core",
        nargs="+",
        type=int,
        default=[1],
        help=(
            "Candidate numbers of CPU cores. Not appended to mdrun automatically; "
            "use {n_core} or MDTBX_N_CORE in launcher/helper scripts if needed."
        ),
    )
    parser.add_argument(
        "--ntomp",
        "--omp",
        nargs="+",
        type=int,
        default=[1],
        help="Candidate OpenMP thread counts",
    )
    parser.add_argument(
        "--ntmpi",
        "--mpi",
        nargs="+",
        type=int,
        default=[1],
        help="Candidate MPI rank counts",
    )
    parser.add_argument(
        "--gpu-env",
        default="cuda_visible_devices",
        choices=["none", "cuda_visible_devices"],
        help="How to expose the selected number of GPUs to each trial",
    )
    parser.add_argument(
        "-o",
        "--output",
        default="opt_perf_best.json",
        type=str,
        help="Output JSON file for the best trial",
    )
    parser.add_argument(
        "--history-output",
        default="opt_perf_history.csv",
        type=str,
        help="Output CSV file for all completed trials",
    )

    parser.set_defaults(func=run)


def _validate_candidates(name: str, values: list[int]) -> list[int]:
    if not values:
        raise ValueError(f"{name} candidates must not be empty")
    if any(v < 1 for v in values):
        raise ValueError(f"{name} candidates must be positive integers")
    return sorted(set(values))


def _grid_size(args) -> int:
    return math.prod(
        [
            len(args.n_gpu),
            len(args.n_core),
            len(args.ntomp),
            len(args.ntmpi),
        ]
    )


def _make_sampler(args):
    if args.sampler == "grid":
        return optuna.samplers.GridSampler(
            {
                "n_gpu": args.n_gpu,
                "n_core": args.n_core,
                "ntomp": args.ntomp,
                "ntmpi": args.ntmpi,
            }
        )
    if args.sampler == "random":
        return optuna.samplers.RandomSampler(seed=args.seed)
    return optuna.samplers.TPESampler(seed=args.seed)


def _resolve_n_trials(args) -> int:
    if args.n_trials is not None:
        return args.n_trials
    if args.sampler == "grid":
        return _grid_size(args)
    return 20


def _resolve_mpi_mode(args) -> str:
    if args.mpi_mode != "auto":
        return args.mpi_mode
    if args.mpi_launcher:
        return "external"
    return "thread_mpi"


def _build_command_template(args) -> str:
    if args.command_template is not None:
        return args.command_template

    command = args.mdrun_command
    if args.mpi_launcher:
        command = f"{args.mpi_launcher} {command}"

    if "{log_path}" not in command:
        command += " -g {log_path}"
    if "{ntomp}" not in command:
        command += " -ntomp {ntomp}"
    if _resolve_mpi_mode(args) == "thread_mpi" and "{ntmpi}" not in command:
        command += " -ntmpi {ntmpi}"
    return command


def _gpu_ids(n_gpu: int) -> str:
    return "".join(str(i) for i in range(n_gpu))


def _format_command(
    args, params: dict[str, int], trial_dir: Path, log_path: Path
) -> str:
    return _build_command_template(args).format(
        trial_dir=trial_dir,
        log_path=log_path,
        n_gpu=params["n_gpu"],
        n_core=params["n_core"],
        ntomp=params["ntomp"],
        ntmpi=params["ntmpi"],
        gpu_ids=_gpu_ids(params["n_gpu"]),
    )


def _completed_trial_records(study: optuna.Study) -> list[dict[str, object]]:
    records = []
    for trial in study.trials:
        if trial.state != optuna.trial.TrialState.COMPLETE:
            continue
        records.append(
            {
                "trial": trial.number,
                "value": trial.value,
                "n_gpu": trial.params["n_gpu"],
                "n_core": trial.params["n_core"],
                "ntomp": trial.params["ntomp"],
                "ntmpi": trial.params["ntmpi"],
                "log_path": trial.user_attrs.get("log_path", ""),
                "command": trial.user_attrs.get("command", ""),
            }
        )
    return records


def _write_history(path: Path, records: list[dict[str, object]]) -> None:
    if not records:
        return
    with path.open("w", newline="") as fh:
        writer = csv.DictWriter(
            fh,
            fieldnames=[
                "trial",
                "value",
                "n_gpu",
                "n_core",
                "ntomp",
                "ntmpi",
                "log_path",
                "command",
            ],
        )
        writer.writeheader()
        writer.writerows(records)


def run(args):
    args.n_gpu = _validate_candidates("n_gpu", args.n_gpu)
    args.n_core = _validate_candidates("n_core", args.n_core)
    args.ntomp = _validate_candidates("ntomp", args.ntomp)
    args.ntmpi = _validate_candidates("ntmpi", args.ntmpi)

    workdir = Path(args.workdir)
    workdir.mkdir(parents=True, exist_ok=True)

    sampler = _make_sampler(args)
    n_trials = _resolve_n_trials(args)
    study = optuna.create_study(direction="maximize", sampler=sampler)

    def objective(trial: optuna.Trial) -> float:
        params = {
            "n_gpu": trial.suggest_categorical("n_gpu", args.n_gpu),
            "n_core": trial.suggest_categorical("n_core", args.n_core),
            "ntomp": trial.suggest_categorical("ntomp", args.ntomp),
            "ntmpi": trial.suggest_categorical("ntmpi", args.ntmpi),
        }

        trial_dir = workdir / f"trial_{trial.number:04d}"
        trial_dir.mkdir(parents=True, exist_ok=True)
        log_path = trial_dir / args.log_name
        command = _format_command(args, params, trial_dir, log_path)

        env = os.environ.copy()
        env["OMP_NUM_THREADS"] = str(params["ntomp"])
        env["MDTBX_N_GPU"] = str(params["n_gpu"])
        env["MDTBX_N_CORE"] = str(params["n_core"])
        env["MDTBX_NTOMP"] = str(params["ntomp"])
        env["MDTBX_NTMPI"] = str(params["ntmpi"])
        env["MDTBX_GPU_IDS"] = _gpu_ids(params["n_gpu"])
        if args.gpu_env == "cuda_visible_devices":
            env["CUDA_VISIBLE_DEVICES"] = ",".join(
                str(i) for i in range(params["n_gpu"])
            )

        trial.set_user_attr("command", command)
        trial.set_user_attr("log_path", str(log_path))

        LOGGER.info(
            "trial=%s n_gpu=%s n_core=%s ntomp=%s ntmpi=%s",
            trial.number,
            params["n_gpu"],
            params["n_core"],
            params["ntomp"],
            params["ntmpi"],
        )

        subprocess.run(command, shell=True, check=True, cwd=trial_dir, env=env)

        parsed = parse_log_file(log_path)
        if parsed is None or parsed["performance"] is None:
            raise ValueError(f"Failed to read performance from {log_path}")
        return float(parsed["performance"])

    study.optimize(objective, n_trials=n_trials)

    best_trial = study.best_trial
    output_path = Path(args.output)
    output_path.write_text(
        json.dumps(
            {
                "value": best_trial.value,
                "params": best_trial.params,
                "log_path": best_trial.user_attrs.get("log_path"),
                "command": best_trial.user_attrs.get("command"),
            },
            indent=2,
            sort_keys=True,
        )
        + "\n"
    )

    history_path = Path(args.history_output)
    records = _completed_trial_records(study)
    _write_history(history_path, records)

    LOGGER.info(f"Best performance: {best_trial.value:.3f} ns/day")
    LOGGER.info(f"Best params: {best_trial.params}")
    LOGGER.info(f"Saved best result to {output_path}")
    LOGGER.info(f"Saved trial history to {history_path}")
