# CLAUDE.md is intentionally a single-line import of AGENTS.md.
ignores:
  - CLAUDE.md
config:
  MD013:
    line_length: 80
    code_blocks: false
    tables: false
  MD033:
    allowed_elements:
      - details
      - summary
